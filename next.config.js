/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['w7.pngwing.com', 'upload.wikimedia.org', 'encrypted-tbn0.gstatic.com']
    }
}

module.exports = nextConfig
